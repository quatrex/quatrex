[convolve]
batch_size = 10_000

[nevp]
eig_compute_location = "numpy"

# Use QR on the GPU for Beyn's method.
use_qr = true
project_compute_location = "numpy"

# Don't use batches for the contour.
# contour_batch_size = None

num_threads_contour = 1024

[lyapunov]
eig_compute_location = "numpy"


[band_edge]
# Use eigvalsh on the GPU for band edge tracking.
use_eigvalsh = true
eigvalsh_compute_location = "numpy"